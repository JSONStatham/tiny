app_name: "tiny"
env: "prod"
debug: false
alias_length: 6
http_server:
  address: "0.0.0.0:80"
  timeout: 4s
  idle_timeout: 4s
db:
  host: ${DB_HOST}
  port: ${DB_PORT}
  user: ${DB_USER}
  password: ${DB_PASSWORD}
  name: ${DB_DATABASE}
